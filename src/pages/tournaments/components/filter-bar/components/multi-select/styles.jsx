import styled from "styled-components";

import { Select, Typography } from "components/material-ui";

export const CustomSelect = styled(Select)`
  .MuiInputBase-input {
    padding: 10px 14px;
  }
`;

export const SmallTypography = styled(Typography)`
  line-height: inherit;
`;
